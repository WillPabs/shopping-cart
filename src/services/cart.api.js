import express from "express";

const cartRoutes = express.Router();

const cartApi = () => {
    
    // create cart
    cartRoutes.route('/cart').get((req, res) => {
        // const dbConnect = dbo.getDb();
    })

    // add to cart

    // delete from cart

    // checkout
}